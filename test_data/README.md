Place environment workbooks here using the naming convention `<environment>_Test_Data.xlsx`, for example `AWS_Test_Data.xlsx`.

Required sheets for the login example:

- `EnvironmentSetUp`: `Browser`, `Url`
- `Login`: `Email`, `Password`

Additional sheets can be added without changing the reader.
